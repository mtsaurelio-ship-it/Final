name: Gerar APK Capacitor Estabilizado
on: workflow_dispatch

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: 1. Checkout do código
        uses: actions/checkout@v4

      - name: 2. Instalar Node.js (v22)
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: 3. Instalar Dependências e Build Web
        run: |
          npm install --force
          npm run build

      - name: 4. Configurar Java 17 (Versão Estável)
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: gradle

      - name: 5. Sincronizar Capacitor com Android
        run: |
          npx cap sync android

      - name: 6. Instalar Gradle 8.4 (Compatibilidade Total)
        run: |
          wget https://services.gradle.org/distributions/gradle-8.4-bin.zip
          sudo unzip -d /opt/gradle gradle-8.4-bin.zip
          echo "/opt/gradle/gradle-8.4/bin" >> $GITHUB_PATH

      - name: 7. Gerar Gradle Wrapper e Permissões
        run: |
          cd android
          /opt/gradle/gradle-8.4/bin/gradle wrapper --gradle-version 8.4
          chmod +x gradlew

      - name: 8. Compilar APK (Debug)
        run: |
          cd android
          ./gradlew assembleDebug

      - name: 9. Disponibilizar APK para Download
        uses: actions/upload-artifact@v4
        with:
          name: app-final-gerado
          path: android/app/build/outputs/apk/debug/app-debug.apk
