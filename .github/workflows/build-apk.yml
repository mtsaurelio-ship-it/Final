name: Build APK Capacitor

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: 1. Checkout do código
        uses: actions/checkout@v4

      - name: 2. Instalar Node.js (v22)
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: 3. Instalar Dependências
        run: |
          npm install --force --legacy-peer-deps

      - name: 4. Build Web (gerar a pasta dist)
        run: |
          npm run build  # Gera a pasta ./dist com index.html

      - name: 5. Sincronizar Capacitor com Android
        run: |
          npx cap sync android  # Sincroniza plugins e assets com a plataforma Android

      - name: 6. Configurar Java 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: 7. Limpar cache do Gradle
        run: |
          cd android
          ./gradlew clean

      - name: 8. Tornar gradlew executável
        run: |
          chmod +x android/gradlew

      - name: 9. Configurar versão do Gradle
        run: |
          cd android
          ./gradlew wrapper --gradle-version 7.5 --distribution-type all

      - name: 10. Compilar APK
        run: |
          cd android
          ./gradlew assembleDebug

      - name: 11. Disponibilizar APK
        uses: actions/upload-artifact@v4
        with:
          name: app-debug-final
          path: android/app/build/outputs/apk/debug/app-debug.apk
